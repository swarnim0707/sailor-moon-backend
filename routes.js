const moduleRoutes = {
    home: "/home",
    favPhases: "/favPhases"
}

const homeRoutes = {
    currentLunarPhase: "/currentLunarPhase"
}

const favPhasesRoutes = {
    getFavPhases: "/favPhasesList",
    editFavPhases: "/editFavPhases",
    readFavPhases: "/readFavPhases"
}

module.exports = { moduleRoutes, homeRoutes, favPhasesRoutes };